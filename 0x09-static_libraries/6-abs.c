#include "main.h"

/**
 * _abs - this fucntions processed the absolute integr value
 *
 * @n: this takes and integer type input for function
 *
 * Return: executes to 0
 */

int _abs(int n)
{
	if (n < 0)
		n = (-1) * n;
	return (n);
}
